# 🔐 Secure Password Generator

A simple Python Flask web app that generates strong random passwords using the `secrets` module.

## 🚀 Features
- Choose password length (8–64 characters)
- Include/exclude uppercase, digits, and symbols
- Generates secure random passwords using cryptographic randomness
- Easy to run locally or deploy online

## 🛠️ How to Run Locally
1. Clone the repo:
   ```bash
   git clone https://github.com/DeviMadhurima/Secure-Password-Generator.git
   cd Secure-Password-Generator
pip install -r requirements.txt
python app.py

